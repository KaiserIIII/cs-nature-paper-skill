# Study profile: ML benchmark

Use for comparative model evaluation. Freeze data splits, model versions,
prompts/configs, tuning budget, seeds and metrics before formal runs. Require
leakage checks, strong budget-matched baselines, uncertainty and failure slices
only when they answer a claim; test-set tuning is a protocol failure.
