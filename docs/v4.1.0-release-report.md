# CS Nature Paper V4.1.0 Release Report

- Release date: 2026-09-20
- Software status: `RELEASED`
- Primary branch: `main`
- Tag: `v4.1.0`

## Delivered outcome

V4.1.0 converts the approved PaperSpine and Nature Tier A candidates into six
real offline advisory providers. The implementation includes selected upstream
source at exact commits, original licenses, hash-bound resource manifests,
local adapters, typed input/output contracts, discovery in the top-level Skill,
PUBLIC_CORE and PRIVATE_ULTRA routing, and semantic end-to-end execution.

PaperSpine publication production invokes the upstream publication-cycle
interface rather than a placeholder or registry check. The positive E2E case
creates an immutable upload directory, bundle manifest, journal-rule receipts,
deterministic ZIP, and SHA-256 archive receipt and returns `BUNDLE_READY`.

## Verification evidence

- Full unit/integration regression: `396 tests`, PASS.
- Focused V4.1 provider and routing suite: `23 tests`, PASS.
- Release validator: PASS.
- Registry validator: PASS.
- Source manifest: `735 files`, PASS.
- Privacy lint: PASS with zero findings.
- Python compile check: PASS.
- Selected-resource installation integrity: six of six providers PASS.
- PUBLIC_CORE semantic E2E: six of six executed; publication production returned
  `BUNDLE_READY`; no formal qualification claimed.
- PRIVATE_ULTRA semantic E2E: six of six executed; publication production
  returned `BUNDLE_READY`; no formal qualification claimed.

The aggregate research `e2e_status` remains `CONDITIONAL` because the broader
multi-system model-behavior comparison and TTA publication sufficiency gates
are still incomplete; this does not negate the separately recorded six-provider
software E2E passes.

GitHub Hosted CI is resolved against the exact release commit by
`scripts/resolve_release_manifest.py`; the release is published only after its
Ubuntu/Windows Python matrix succeeds.

## Qualification boundary

This is a stable software release, not a claim that selected external providers
are formal scientific evidence authorities. The trust-bound qualification
record remains `PENDING_FINAL_REVIEW`; provider qualification is `UNAUDITED`,
comparison is `NOT_RUN`, and eligibility is `ADVISORY_ONLY`. Formal requests
continue to return the internal fallback with `FORMAL_EVIDENCE_INCOMPLETE`.

AREX remains discovery-only; Curie and CORAL remain adapter candidates;
SkillLens remains methodology-only; POPPER remains reference-only. No bulk
catalog or unrelated Skill was installed.
