# V3.2.0 Release Candidate Report

Date: 2026-08-28  
Branch: `feat/cumcm-v32-final`
Baseline: `v3.2` / `56888d76197df86862ae5642f0134fa6e3d88fc8`

## Scope

V3.2 adds maximum-autonomy execution around the v3.1.1 research control and
execution planes. Ordinary reversible work is automatic; bounded higher-impact
work is audited; fundamental author-only and unsafe work remains blocked. The
candidate includes a complete isolated AUTO_HIRE lifecycle, evidence-bound node
executors, per-node self-healing budgets, project `READY_FOR_SUBMISSION`
completion semantics, and separate software release integrity. The CUMCM
runtime is integrated as a clock/risk/ETA overlay that reuses authorization,
execution, evidence, audit, recovery, and graph contracts.

The final CUMCM specialization adds a real Competition Director that reads and
decomposes every supplied problem, makes a qualitative automatic selection
when one option clearly dominates, routes mixed methods, executes a baseline
and formal solver, verifies feasibility/units/numbers, runs sensitivity,
generates traceable figures and a Chinese paper, performs judge-style review
and automatic repair, and stops at `COMPETITION_SUBMISSION_READY`. Temporary
clock and ETA restrictions remain policy projections rather than canonical
scientific graph transitions.

## Scientific integrity

The v3.1.1 provenance, evidence, research graph, scientific gates, privacy and
security pressure behavior, and regression tests remain authoritative. Declared
or pilot artifacts cannot be upgraded to formal evidence. The full-paper run is
classified `HARNESS_SELF_TEST`; it is not a model behavior evaluation, venue
claim, publication result, or scientific generalization. No third-party
dependency or Skill was installed, and no external write, publication,
submission, tag rewrite, or main merge was performed.

## Deterministic gate record

The final report records fresh output for the following commands on the final
candidate commit:

```text
python -m unittest discover -s tests -p 'test*.py' -v
python scripts/validate_release.py
python scripts/validate_registry.py
python scripts/build_manifest.py --check
python scripts/privacy_lint.py
python scripts/security_pressure_run.py --output .security-pressure-run
python scripts/smoke_run.py --output .ci-smoke-result.json
python scripts/check_smoke.py .ci-smoke-result.json
python scripts/competition_smoke_run.py --output .competition-smoke-result.json
python scripts/competition_orchestration_e2e.py --output .competition-orchestration-e2e.json
python scripts/full_paper_e2e.py --output .full-paper-e2e.json
python scripts/validate_release.py --v32-e2e .full-paper-e2e.json
python scripts/validate_release.py --competition-e2e .competition-orchestration-e2e.json
```

Generated runtime files are CI-only and excluded from release-controlled
checksums. Hosted model behavior remains `NOT_RUN` when no authorized isolated
adapter is available.

## Disposition

The committed `release_manifest.json` is intentionally `V3.2.0 RELEASE
BLOCKED`. Only the Hosted CI aggregation job, after all six OS/Python matrix
jobs pass, may emit a resolved manifest whose source commit, E2E commit,
workflow, branch, CI head SHA, and matrix are identical. The historical
`v3.1.1` tag is unchanged.

## Local verification evidence

The final local deterministic gate produced:

- `149` unit and integration tests: `OK`; the focused competition suite is
  `55/55 PASS`.
- Release/schema, registry, manifest, privacy, and stale-commit checks: `PASS`.
- Security pressure harness: `PASS` for all 11 fail-closed cases.
- Legacy smoke: `PASS`, `HARNESS_SELF_TEST`, 21 graph events and 19 anchors.
- Director orchestration E2E: `PASS`, 20 normal-runtime node executions, 20
  observed anchors, real command-produced formal output, and
  `ordinary_author_prompts = 0`.
- CUMCM infrastructure smoke: `PASS`; temporary freeze/ETA blocks remain
  overlay state and do not alter canonical scientific status.
- Competition orchestration E2E: `PASS`; the normal Director completes all 16
  nodes with real command-produced output, `ordinary_author_prompts = 0`, one
  automatic review/repair cycle, strict preflight, and ten fail-closed negative
  cases.
- Model behavior evaluation: `NOT_RUN`. Model-backed end-to-end autonomous
  research behavior has not been independently validated.
