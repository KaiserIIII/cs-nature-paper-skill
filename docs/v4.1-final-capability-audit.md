# V4.1 Final Capability Audit and Candidate-Pool Freeze

**Audit date:** 2026-09-19  
**Working branch:** `feat/v4.1-specialist-integration`  
**V4 baseline:** `b71b892b5277c0d2cd7dbdf7cd7c4da732c03796`  
**Baseline regression:** `275 passed, 24 subtests passed`  
**Release posture:** fail-closed; `RC_NOT_RELEASED`; behavior benchmark not run

## Decision summary

The final bounded expansion round is complete. The candidate pool is frozen in
[`assets/registry/v41_candidate_pool_freeze.json`](../assets/registry/v41_candidate_pool_freeze.json).
The audit covers only capabilities that could fill a real gap or improve
qualification methodology; no repository was selected because of popularity,
size, agent count, or marketing claims.

V4.1 remains a subordinate integration layer. The Research Control Plane is the
only authority for RQs, claims, evidence, protocol, amendments, graph
transitions, provenance, publication gates, permissions, and release. No
candidate has formal eligibility because no comparable behavior run has been
performed.

## Audited candidates

The machine-readable source record is
[`assets/registry/v41_upstream_audits.json`](../assets/registry/v41_upstream_audits.json).

| Candidate | Exact audited commit | Classification | Accepted V4.1 scope | License/security decision |
|---|---|---|---|---|
| PaperSpine5 (`WUBING2023/PaperSpine`) | `99a84fe452ab7681e30cd81387fcb6263a676125` | `INTEGRATE_NOW` | Contribution, Results-as-Validation, target-exemplar, publication-production bounded providers | MIT; selected-resource adapter only; runtime/UI/installer excluded |
| Nature Skills (`Yuan1z0825/nature-skills`) | `9cecfef6ac683fa59d7d15d2e22f98fa71dacaf5` | `INTEGRATE_NOW` | `nature:figure`, `nature:reviewer` | Apache-2.0; preserve attribution/notices; no network/install path in formal execution |
| AREX-Skill (`VectorSpaceLab/AREX-Skill`) | `ac3fe1afa80fb9a09775ecfb2b6cc3ba850a2db6` | `ADAPTER_CANDIDATE` | `AREX_DISCOVERY_PROVIDER` only | Apache-2.0 repository is not enough; each discovered Skill needs its own license, pin, static/security/behavior/checker qualification |
| Systematic Literature Review (`yananlong/codex-skills`) | `955fcc435e651834b03768c136dca6b16ccbae08` | `METHODOLOGY_ONLY` | native `literature_recall_assurance` challenger for high-risk completeness claims | no repository license detected; no code vendored |
| SkillLens (`SkillLens-AI/skilllens`) | `ce8fcb89b00164569ed792d8737ee1315b070556` | `METHODOLOGY_ONLY` | utility/security-separated, repeated-rollout qualification methodology | Apache-2.0 code/NOTICE and CDLA-Permissive dataset references; runtime/datasets not vendored |
| Curie (`Just-Curieous/Curie`) | `db1b1f56159b591515f77e03c55bf473d5c1c201` | `ADAPTER_CANDIDATE` | subordinate `curie:experiment-backend` | Apache-2.0; Docker/subprocess/dependency surface quarantined behind frozen V4 contract |
| CORAL (`Human-Agent-Society/Coral`) | `0123dfb939b35228cf2c1fde224cd0561e727408` | `ADAPTER_CANDIDATE` | subordinate `coral:parallel-search-backend` | Apache-2.0; worktree/process/grader mutation requires isolation and checker |
| POPPER (`snap-stanford/POPPER`) | `59611864e09dd15d7528e240ad87c7ddbb4a701c` | `REFERENCE_ONLY` | high-level source for native `falsification_obligation` | no repository license detected; no implementation copied |
| Autoresearch Paper (`WdBlink/autoresearch-paper`) | `ad40dcc605a834b879d2dc43b0d04de5cb4e0346` | `REFERENCE_ONLY` | runtime-invariant adversary only | MIT; no second control plane or duplicate runtime merged |

## Capability-gap matrix

| Proposed capability | Genuine gap | Existing V4 overlap | Incremental value | Routing |
|---|---|---|---|---|
| Contribution/results traceability | contribution acceptance criteria were not a first-class derived object | claims/evidence/writing gates | makes contribution and result-unit boundaries explicit | Tier A bounded provider |
| Nature figure QA | journal-oriented source-data/panel/final-size QA | visualization department | catches geometry/accessibility/package defects without changing evidence | Tier A bounded provider |
| Nature reviewer packet/isolation | explicit same-packet reviewer provenance | adversarial review board | makes isolation and finding anchors auditable | Tier A bounded provider |
| `literature_recall_assurance` | completeness assurance, not just paper lookup | literature/novelty/evidence | targeted recall challenge for novelty-critical decisions | methodology/advisory |
| AREX discovery | discover a missing niche capability without bulk trust | capability resolver/marketplace | provenance-aware discovery only | discovery-only |
| SkillLens methodology | utility and security are not the same qualification question | eval runner/release gates | repeated rollouts, fixed judges, exact hashes and run metadata | evaluation methodology |
| Curie backend | external experiment execution option | experiment planner/job runtime | candidate execution under frozen contract | system adapter/benchmark |
| CORAL backend | parallel implementation/search exploration | host multi-agent execution | candidate alternatives under frozen objective | system adapter/benchmark |
| `falsification_obligation` | explicit claim-level attempts to disconfirm and bound claims | innovation/design/reviewer threats | reduces confirmation bias with profile-aware obligations | native V4 graph/evidence concept |

## Qualification and security policy

The provider record uses three independent states:

- `qualification_status`: `UNAUDITED`, `AUDITED`, `PROVISIONAL`, `QUALIFIED`,
  `REJECTED`;
- `comparison_decision`: `NOT_RUN`, `INTERNAL_BETTER`, `EXTERNAL_BETTER`,
  `COMPLEMENTARY`, `EQUIVALENT`, `INCONCLUSIVE`;
- `eligibility`: `DISCOVERY_ONLY`, `ADVISORY_ONLY`, `NON_LOAD_BEARING`,
  `FORMAL_ELIGIBLE`, `DISABLED`.

`eligibility` is derived, never hand-promoted. Formal eligibility requires an
exact pin and source hash, acceptable license/security decisions, behavior
qualification, a typed output contract, an independent checker, and either
`EXTERNAL_BETTER` or verified `COMPLEMENTARY` value against `PUBLIC_CORE`.

Utility and security are also separate. Security checks include arbitrary shell,
package installation, runtime clone/download, secret access, credential
leakage, uncontrolled network, data egress, file writes/destructive behavior,
hidden telemetry, and subprocess scope. A useful provider with an unsafe
runtime remains ineligible.

## Accepted design deltas

The V4.1 design now records:

1. Discovery / Evaluation, System Adapter, and Reference / Methodology tiers.
2. The exact three-state provider model above.
3. SkillLens-derived benchmark metadata: run ID, timestamps, provider/V4/source
   hashes, model and prompt/config hashes, toolset/network policy, budgets,
   sampling/seed metadata, retries, artifacts, checker/judge identities and
   config hashes, failures, duration, and multiple rollouts where practical.
4. Dedicated `review-synthesis-provider`/process output followed by an
   independent `review-synthesis-checker` that validates but never generates,
   repairs, or rewrites the artifact it checks.
5. Native, profile-aware `falsification_obligation` rather than a POPPER
   runtime import.
6. Offline `PUBLIC_CORE` fallback and fail-closed behavior for missing,
   stale, unsafe, unlicensed, or behavior-unqualified candidates.

## Explicit non-integrations

- AREX's bulk catalog, auto-install/download/execute behavior, and repository-
  level license inheritance.
- The systematic-review repository's unlicensed implementation.
- SkillLens runtime and datasets.
- Curie or CORAL control-plane logic, RQ/protocol mutation, or evidence
  promotion.
- POPPER implementation and Autoresearch Paper control-plane/runtime code.
- Additional Nature Skills beyond the bounded figure/reviewer scope.
- Daily-push/logging infrastructure, product UI, installers, update systems,
  and unrelated desktop/web applications.

## Benchmark plan and release posture

No behavior benchmark was run or inferred from architecture. The planned
counterbalanced benchmark covers figure construction/audit,
argument reconstruction, contribution-to-results mapping, statistical audit,
literature evidence, adversarial review, and publication-package construction.
Until real behavior evidence exists, `comparison_decision` remains `NOT_RUN`,
no candidate can become `EXTERNAL_BETTER`, and release remains fail-closed.

## Duplicate installation

The legacy duplicate installation remains untouched except for its pre-existing
local `SKILL.md` modification. It was not deleted, moved, or re-identified in
this audit. Any future routing change must first save `git status`, `git diff`,
and a patch/backup, then verify exactly one active `cs-nature-paper` resolution.

## Required answers

1. **PUBLIC_CORE-only operation?** Yes. V4.1 remains fully usable offline with
   `PUBLIC_CORE`; external systems are optional quality upgrades.
2. **Formal eligibility without behavior evidence?** No. None received it;
   all comparisons are `NOT_RUN`.
3. **External control-plane authority?** No. All candidates are subordinate
   artifact producers, adapters, or methodology references.
4. **AREX trust automatic?** No. AREX is discovery-only; every discovered
   Skill is independently pinned, licensed, audited, trialed, and checked.
5. **Unlicensed code vendored?** No. The two no-license candidates are
   methodology/reference-only and no upstream code was copied.
6. **Curie/CORAL subordinate backends?** Yes. They accept frozen V4 contracts
   and cannot redefine protocol, claims, evidence, or authority.
7. **Falsification status?** Represented as a native, profile-aware V4
   `falsification_obligation`; POPPER remains reference-only.
8. **Candidate pool frozen?** Yes: `V4.1_CANDIDATE_POOL_FROZEN = true`.
9. **Further provider search needed?** No. Further candidates belong to V4.2
   unless a specific critical security/functionality exception is recorded.

## Implementation readiness

The audit/design phase is complete and the implementation can now begin in the
isolated branch under TDD. The next phase must add contracts, adapters, tests,
and benchmark fixtures while preserving the fail-closed release manifest. This
audit intentionally does not claim that V4.1 is implemented, behavior-qualified,
or formally released.
