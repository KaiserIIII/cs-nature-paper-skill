# V4.0.0 RC report

Generated: 2026-09-08 (Asia/Hong_Kong)
Branch: `feat/v4-vendored-research-team`
Base commit: `6f13161601854763b500cdb596dbe52df3a0fd19`
Release status: `RC_NOT_RELEASED`

## What is implemented

V4 includes a built-in 14-role research team, pinned and audited permissive
third-party research Skills, built-in-first routing with quality-upgrade
discovery, typed provider handoffs, publication sufficiency and reviewer
completeness gates, hash-bound experiment expansion, checkpoint/resume/retry
support, and a 17-system behavior benchmark suite.

The benchmark suite covers 17 skill, plugin, tool, and system targets including
ARS, K-Dense Scientific Agent Skills, AI Scientist, PaperOrchestra, Sisyphus
Academica, and Research Engineering Suite. Each has an immutable commit,
license decision, public behavior task, parity dimensions, and V4 superiority
dimensions. Evaluation requires completed producer artifacts, SHA-256 binding,
two blind counterbalanced judge orders, and evidence for every scored dimension.
Agent count, repository size, skill count, and architecture diagrams are
prohibited proxies.

## Deterministic verification

- V4 focused tests: **36 passed** (`tests/test_v4_release.py`, internal specialists,
  vendor runtime, publication gates, benchmark suite, research expansion).
- Release validator: **PASS** (`python scripts/validate_release.py --manifest release_manifest.json`).
- Benchmark schema validator: **PASS** (`python scripts/research_os_benchmark.py validate`).
- Full pytest: **275 passed, 24 subtests passed**. No test failures were observed
  in the current V4 working tree.

## Benchmark gate

The 17 real comparator runs have not produced complete output and blind-judge
artifacts. The release manifest therefore correctly reports:

```text
benchmark_suite.status = NOT_RUN
ARS_PARITY_GATE = FAIL
V4_SUPERIORITY_GATE = FAIL
recommended_merge = NO
```

No parity or superiority claim is made from structure or agent-count proxies.

## TTA field expansion

Project: `external-project:ccta_tta` (local working copy; runtime status is kept outside this repository)

The prior 960-job toy study remains publication-insufficient and its invalid
prospective predictor claim is withdrawn. The expansion protocol covers
CIFAR-10/CIFAR-100, five seeds, five methods/controls, two trajectories, five
shift families, two severities, modern-baseline reproduction targets, mechanism
measurements taken before return outcomes, ablations, uncertainty, and
adversarial review.

- CUDA environment verified: Python 3.13, torch 2.9.1+cu128,
  torchvision 0.24.1+cu128, CUDA 12.8, NVIDIA GeForce RTX 4060 Laptop GPU.
- GPU smoke: **COMPLETED**, 2/2 jobs, 0 failures, SHA-bound outputs.
- Formal campaign: **COMPLETED** with 1000/1000 jobs, four workers,
  checkpoint/resume/retry, zero current failures, and preserved historical
  failures. The final status is persisted in
  `ccta_tta/run_state/expansion_formal_v2_status.json` and the postprocess
  record in `ccta_tta/run_state/formal_v2_postprocess.json`.
- Admission and postprocess: **PASS**. The analysis contains 1,000 verified
  rows and records the single frozen-runner evidence-label alias without
  modifying raw shards; the complete recovery package was synced to the F:
  drive.
- The mechanism screen is **NOT_SUPPORTED** because it has one held seed and
  negative incremental fit. Fresh held seeds 5-9 and a preregistered
  prospective analysis remain required. Publication sufficiency and reviewer
  completeness remain **FAIL**.

## Release disposition

`V4.0.0 RC FAIL`. Required multi-system behavior evidence, publication
sufficiency, reviewer completeness, and exact-SHA Hosted CI remain pending. The
feature branch is pushed to GitHub for review; it has not been merged, tagged,
or released.
